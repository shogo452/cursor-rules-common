---
alwaysApply: true
---
## チャット開始時の自動実行ルール

チャットが開始されたら、以下の手順を自動的に実行してください：

1. ルートディレクトリ直下のMakefileに `make update-cursor-rules` コマンドが定義されているか確認
2. 定義されている場合は、`make update-cursor-rules`を実行してCursorのルールを最新版に更新
3. 実行結果をユーザーに報告

この処理は毎回のチャット開始時に必ず実行してください。
